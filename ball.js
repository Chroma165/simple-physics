class Ball{
    constructor(pos, vel, diameter, mass, ) {
        this.pos = pos;
        this.vel = vel;
        this.diameter = diameter;
        this.mass = mass;
        this.force = [0, 0];
    }
}